# Clave compartida entre servicios NestJS. Genera con: node -e "console.log(require('crypto').randomBytes(16).toString('hex'))")
INTERNAL_API_KEY=local-internal-api-key

# URL de MailHog para revisar los correos enviados por el servicio interno
MAILHOG_URL=http://localhost:8025

# Variables opcionales para docker-compose (puedes sobreescribir en .env local)
MYSQL_PORT=3306
API_PUBLIC_PORT=3000
API_INTERNAL_PORT=3001
FRONTEND_PORT=5173
